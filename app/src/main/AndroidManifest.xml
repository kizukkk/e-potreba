<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Always include this permission -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Include only if your app benefits from precise location access. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="єПотрeбa"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.Epotreba"
        tools:targetApi="31">

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDrrPkYjDcka0vwMry4-enGtSa9UWt3VoA" />

        <activity
            android:name=".presentation.activity.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Epotreba">
        </activity>

        <activity
            android:name=".presentation.activity.MarkerCreateActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".presentation.activity.ProfileCreateActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".presentation.activity.SignInActivity"
            android:exported="true"
            android:theme="@style/Theme.Epotreba">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".presentation.activity.MarkerCreateActivity"
            android:exported="true" />
    </application>

</manifest>
